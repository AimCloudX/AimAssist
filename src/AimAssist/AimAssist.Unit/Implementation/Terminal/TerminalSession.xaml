<UserControl x:Class="AimAssist.Units.Implementation.Terminal.TerminalSession"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Terminal Display -->
        <Border Grid.Row="0" Background="Black" BorderBrush="#333" BorderThickness="1">
            <ScrollViewer Name="scrollViewer" VerticalScrollBarVisibility="Auto" 
                          HorizontalScrollBarVisibility="Auto">
                <TextBox Name="txtTerminal" 
                         Background="Black" 
                         Foreground="#00FF00"
                         FontFamily="Consolas"
                         FontSize="12"
                         IsReadOnly="True"
                         BorderThickness="0"
                         TextWrapping="NoWrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Disabled"
                         HorizontalScrollBarVisibility="Disabled"
                         Padding="5"/>
            </ScrollViewer>
        </Border>
        
        <!-- Input Area -->
        <Grid Grid.Row="1" Background="#1E1E1E" Height="30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text=">" Foreground="#00FF00" 
                       FontFamily="Consolas" FontSize="12" 
                       VerticalAlignment="Center" Margin="5,0"/>
            <TextBox Grid.Column="1" Name="txtInput" 
                     Background="#1E1E1E" 
                     Foreground="#00FF00"
                     FontFamily="Consolas"
                     FontSize="12"
                     BorderThickness="0"
                     KeyDown="TxtInput_KeyDown"
                     Margin="0,2"
                     VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</UserControl>